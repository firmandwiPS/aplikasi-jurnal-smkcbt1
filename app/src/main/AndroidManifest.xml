<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Apkjurnalsmkcbt1"
        tools:targetApi="31">

        <activity android:name=".layout_navigasi.SiswaActivity" android:exported="false" />
        <activity android:name=".layout_navigasi.OrtuActivity" android:exported="false" />
        <activity android:name=".layout_navigasi.GuruActivity" android:exported="false" />
        <activity android:name=".layout_navigasi.KepsekActivity" android:exported="false" />
        <activity android:name=".layout_navigasi.MainActivity" android:exported="false" />

        <!-- LoginActivity sebagai launcher -->
        <activity android:name=".login.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
